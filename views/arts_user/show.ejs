<!-- <% layout("layouts/boilerplate") %>

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="user-show-card">
                <img src="<%= artInstance.image %>" class="card-img-top cursor-pointer" alt="art image" 
                     />
                <div class="card-body">
                    <h2 class="card-title mb-3"><%= artInstance.title %></h2>
                    <p class="card-text mb-4"><%= artInstance.description %></p>
                    <div class="art-details mb-4">
                        <p class="card-text">
                            <strong>Artist:</strong> <%= artInstance.artistName %>
                        </p>
                        <p class="card-text">
                            <strong>Category:</strong> <%= artInstance.category %>
                        </p>
                        <p class="card-text price-tag">
                            <strong>Price:</strong> ₹<%= artInstance.price.toLocaleString("en-IN") %>
                        </p>
                    </div>
                    <div class="d-flex justify-content-between align-items-center">
                        <a href="/arts-user" class="btn btn-info">Back to Gallery</a>
                        <a href="/arts/<%= artInstance._id %>/buy" class="btn btn-success">Buy Now</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

Add Modal for Full Image
<div id="imageModal" class="image-modal">
    <span class="close-modal">&times;</span>
    <img id="modalImage" class="modal-content" src="" alt="Full size image">
</div>

Add JavaScript for modal functionality
<script>
    const modal = document.getElementById("imageModal");
    const modalImg = document.getElementById("modalImage");
    const closeBtn = document.getElementsByClassName("close-modal")[0];

    function openImageModal(imgSrc) {
        modal.style.display = "block";
        modalImg.src = imgSrc;
    }

    // Close modal when clicking the × button
    closeBtn.onclick = function() {
        modal.style.display = "none";
    }

    // Close modal when clicking outside the image
    modal.onclick = function(event) {
        if (event.target === modal) {
            modal.style.display = "none";
        }
    }

    // Close modal when pressing Escape key
    document.addEventListener('keydown', function(event) {
        if (event.key === "Escape") {
            modal.style.display = "none";
        }
    });
</script> -->

<% layout("/layouts/boilerplate") %>

<div >

  <div class="container mt-3 "></div>
    <div class="row"></div>
      <div class="col-12 text-center">
        <h1 class="display-8">
          <b><%= artInstance.title %></b>
        </h1>
      </div>
    </div>
    <br>
<div class="mt-3">

      <div class="col-md-8 offset-md-2 ">
         <div class="card d-flex justify-content-center align-items-center" style="border: none !important;">

          <img src="<%= artInstance.image %>" class="card-img-top" alt="Listing_image">
          
          <br>
          <div class="card-body">
            <h3 class="card-title mt-3 d-flex justify-content-center"><%= artInstance.description %></h3>

            <h5 class="card-text mt-3 d-flex justify-content-end"> ~ By <i><%= artInstance.artistName %></i></h5>

            <h5 class="card-text">Price: &#8377; <%= artInstance.price.toLocaleString("en-IN") %></h5>

            <h5 class="card-text">Category: <%= artInstance.category %></h5>

            
            
            <div class="d-flex justify-content-between align-items-center">
                <a href="/arts-user" class="btn btn-info">Back to Gallery</a>
                <a href="/arts/<%= artInstance._id %>/buy" class="btn btn-success">Buy Now</a>
            </div>
            <br><br>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

